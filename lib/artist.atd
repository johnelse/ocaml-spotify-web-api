type artist = {
  href: string;
  id: string;
  name: string;
  uri: string;
}

type search = {
  href: string;
  items: artist list;
  limit: int;
  offset: int;
  total: int;
}

type search_wrapper = {
  artists: search;
}
